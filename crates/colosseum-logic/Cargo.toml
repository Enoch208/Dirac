[package]
name = "colosseum-logic"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
sha2 = { version = "0.10", default-features = false }
libm = "0.2"

[dev-dependencies]
